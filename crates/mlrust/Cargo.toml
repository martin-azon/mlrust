[package]
name = "mlrust"
version = "0.1.0"
edition = "2024"

[dependencies]
ml_kem = { path = "../ml_kem", default-features = false }

[features]
default = ["std", "getrandom"]
std = ["ml_kem/std"]
getrandom = ["ml_kem/getrandom"]
