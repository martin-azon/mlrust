[package]
name = "ml_kem"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = [
    "mlrust_core/std",
]


[dependencies]
mlrust_core = {path = "../mlrust_core", version = "1.0.0", features = ["getrandom"] }

[dev-dependencies]
hex = "0.4"